"""Benchmark + diagnostic tooling for Aelunor turn runtime.

Importable as a package so the pure helpers (e.g. the llama.cpp failure
classifier) can be unit-tested offline without spinning up an LLM server.
"""
